// Firebase authentication is deactivated in favor of direct master password protection.
// Empty placeholder exports maintained for compatibility if needed.

export const auth = null;
export const db = null;
